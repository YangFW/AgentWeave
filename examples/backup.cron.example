# 示例：使用前确认维护窗口、项目路径、外部备份存储和本机时区。
# 不自动安装到 crontab；运行用户必须能访问 Docker 和数据目录。
0 3 * * * AGENTNEXUS_BACKUP_DIR=/path/to/backup-storage /bin/sh /path/to/AgentNexus/scripts/backup-maintenance.sh --maintenance-window-approved
