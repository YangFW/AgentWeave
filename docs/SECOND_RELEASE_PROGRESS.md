# 第二次上线实施记录

本文件记录实现证据，不替代 `SECOND_RELEASE_PLAN.md` 的完整范围和验收标准。2026-09-09 已经用户授权完成本机多人版部署和每日维护备份安装，真实模型验收及用户试运行尚未完成。下方保留实施过程记录，当前状态以 `SECOND_RELEASE_AUDIT.md` 和 `SECOND_RELEASE_DEPLOYMENT.md` 为准。

## 2026-09-10 上线后复核与真实模型补验

- 首次每日 03:00 的 cron 维护已执行，快照 `backups/agentnexus-20260909T190004Z-3672844` 共 831 文件，重新校验通过；09:10 检查服务正常、两个 Worker 在线。
- 使用独立临时数据库、公开样例及已选 gpt-5.5 配置补跑三项真实模型场景。旅行计划的 Word/PPTX 生成通过；Markdown 第二轮长文档请求触及配置的 90 秒总超时。原专家测试虽然显示通过，实际结果是询问“附件”，没有完成成员评审，因此不计作成功的专家协作验收。
- 修复模型/HTTP/MCP 总超时错误为空的问题：总超时保留所属调用类型及秒数，模型错误能按 ModelCallTimeout 识别。只转换当前 deadline 到期，不改写操作内部主动抛出的超时。
- 补齐目标识别的附件上下文，以及专家成员/主管任务对父任务已授权附件的继承。新增回归验证目标识别收到附件内容、三个子任务继承附件；加强真实专家测试，必须完成团队执行事件，不能以要求补充资料代替。
- 49 项调用边界、专家团、记忆、知识库和模型预算相关回归通过，41.386 秒。真实模型复验在另一隔离实例使用 180 秒模型超时和 300 秒测试等待限额；生产模型仍保持 90 秒，未直接修改用户限制。复验目录 `/tmp/agentnexus-real-model-recheck-qxe5nnt1`。
- 180 秒隔离配置下，Markdown 两轮生成及结构复用通过。专家团已实际启动成员，其中一名成员因未满足样例三类状态标签和篇幅要求被语义验收拒绝，首次严格专家测试失败。随后通过公开单成员重试接口仅重试该成员一次，34.853 秒后团队完成，已成功成员保持一次执行，主管实际汇总完成。该结果保留首次失败事实，不等同于首次全绿；记录见同目录 `retry-result.json`。
- 上下文、恢复、运行中指令和任务 API 追加回归：76 项无失败，1 项 Redis 条件测试未启用，97.509 秒。新镜像 Compose 的启动、任务、备份恢复验证 2 项通过，38.906 秒。
- 无活动任务时生成升级前快照 `backups/context-hotfix-20260910T015404Z`，831 文件校验通过；保留旧镜像标签后将 API/两个 Worker 更新为 `sha256:f6e62c59b05ee20f542dcb9bdbddb6d43038a7e68bb5e999b0ac9853bf915588`。发布后页面 200、未认证任务访问 401、readiness 正常且两个 Worker 在线。
- 生产模型仍为 90 秒，是否改为 180 秒已向用户询问，未收到选择前不改动。两个真实模型临时实例均停止，临时数据库中的模型凭据已清除，结果与失败/重试记录保留。不能把隔离配置的 180 秒结果当作线上已调参。

## 2026-09-09 实际部署已执行

- 用户回复“确认授权”后，完成运行数据检查、完整快照、旧配置和 Git HEAD 归档；没有活动任务需要中断。
- 校验后迁移 486 条历史上传路径和 327 条产物路径，保留文件、账号和加密密钥。使用已验证镜像启动认证 API、Redis 和两个 Worker，单用户并发 1，仅监听 127.0.0.1:8000。
- 既有 admin 登录、历史产物哈希、全部历史文件在容器内可读、新 Markdown 任务交付均通过；新增验收任务 `task_0a473794e68d`，未新增测试用户。
- 在保留原 crontab 的基础上安装每日北京时间 03:00 维护备份；实测停止 API/Worker、生成快照、自动恢复并再次检查登录和 readiness。部署前快照 829 文件、维护快照 831 文件，均校验通过。
- 配置、凭据和备份均留在 Git 忽略范围内。详细路径、回执、日志和运行状态见 `SECOND_RELEASE_DEPLOYMENT.md`。没有进行 Git 提交。

## 已有实现和局部验证

- 独立 AOF Redis 容器重启测试通过：未确认消息、消费组 pending 状态和去重记录均保留，重新领取后确认并清理。测试容器已停止，未影响现有 Redis。

- 新镜像通过真实 Compose 备份恢复演练：停止临时 API/Worker，备份并恢复到新目录，重建容器后旧登录会话、已完成任务和产物下载仍可用，下载字节一致。临时服务、网络和卷已清理；未操作用户运行库。

- 普通只读成员的真实浏览器验收通过：管理入口隐藏、用户管理接口返回 403、工作台提交被服务端拒绝且不创建任务。修复提交失败后文本丢失，失败时恢复原文本但不覆盖新草稿。

- 当前代码版本重新通过 16 项独立 Redis/真实 Worker/Chromium 集成测试；重新构建的镜像通过独立 Compose 的非 root、只读、登录、上传及 Markdown 交付测试。运行库测试账号已于 2026-09-09 经用户确认完成备份、密码重置和旧会话撤销，复核通过。

- 私有智能体在列表、任务提交、自动化配置和 Worker 执行前统一按用户/工作区可见性检查，不能通过直接指定 ID 绕过。前端智能体列表携带当前工作区，相关回归通过。

- 专家模板/团队创建的组织与所有者改用会话身份；普通成员不能发布或提升为组织/公开共享资源。相关认证及专家回归通过。

- 对话中的 Skill/MCP 安装和平台生成器配置增加管理员校验；普通成员不再收到自动安装推荐，已存在推荐的批准也重新校验角色。普通成员安装请求不会调用远端加载器；聊天安装/推荐回归通过。安装写事务已复查当前管理员权限；模拟下载期间角色撤销的测试确认不落库，任务明确失败。

- 普通/专家模式的 `/api/tasks` 支持 Idempotency-Key：相同用户/键/内容复用原任务与运行，冲突内容返回 409；前端同页失败重试复用键，成功后清除。自定义请求头不覆盖 JSON Content-Type。对应 API/前端测试通过；页面刷新后的重试键持久化未实现。

- Worker 在 Redis 连接/运行租约中断后等待重连，编程异常仍失败退出。Redis 丢失投递标记后可补投仍为 queued 的普通/团队运行，不接管 running 运行。12 项 Redis/重连测试通过；活动执行及审批/自动化消息的 Redis 全量丢失恢复仍待覆盖。

- 使用本机 Chromium/CDP 和临时数据库完成管理员登录、创建独立用户、添加只读项目成员及退出的真实浏览器测试，无未捕获脚本异常；检查桌面截图并修正折叠侧栏账号区布局。普通用户全流程、移动布局仍需扩大验收。

- Redis 原子用户配额已接入普通、团队、审批及自动化任务；默认每用户同时 2 个顶层执行，容量不足保留消息。11 项 Redis/Worker/Compose 测试通过，包含并发竞争及用户隔离。

- 附件接收不再静默截断：最多 10 个不同附件，默认合计 40MB，失效 ID 明确拒绝；拒绝请求不创建部分任务。相关 API/Compose 回归通过。

- 自动化触发新增 automation_dispatch 持久投递及单自动化活动记录唯一约束，Worker 执行原有轮次/重试/通知逻辑；API 重启跳过在 Worker 运行的自动化。21 项调度/outbox 测试及真实进程中的单轮自动化通过。进程崩溃、Webhook 重放和自动化审批后续状态仍需完整演练。

- Worker 执行前复查账号启用状态和工作区执行权限；续租时（每 10 秒）重新检查，撤销权限则加入持久化取消命令。权限及取消命令测试通过；已经开始的外部副作用不能自动撤销。

- Worker 单次执行增加 `APP_TASK_TIMEOUT_SECONDS`（默认 1800 秒）上限；到期取消协程并提交 TaskTimeout 失败终态，不作为进程中断自动重试。终态一致性测试通过；线程/子进程的强制终止仍受后述限制。

- 真实进程测试：临时 SQLite、独立 Redis 7、API 和两个 Worker。两项离线任务在无 Worker 时排队，API 重启后启动 Worker，均完成且各仅一条 Run/最终回答。此测试未覆盖执行中杀死 Worker 和外部写工具。
- 增加 `/api/readiness`，按队列隔离 Worker 心跳；无 Worker 返回 503，心跳上线后返回 200。9 项真实 Redis/Worker 测试通过，包括心跳过期及跨队列隔离。

- SQLite 用户和会话；管理员用户创建、更新、停用，修改密码或角色撤销会话；保护最后一个管理员。
- HTTP 登录、注销、会话检查；浏览器登录入口、会话过期跳转和退出。
- 会话身份通过请求上下文传递；普通用户任务列表不接受伪造的用户身份。
- 任务读取、事件和事件流、运行信息、取消/重试/恢复、产物读取/预览/下载增加任务所有权校验。
- 普通任务创建使用会话身份；附件上传登记所有者，普通任务拒绝使用他人附件。
- 模型、Skill、MCP、Agent、Policy 等配置修改增加管理员门禁。
- 工作区成员表、成员管理接口及所有者/成员/只读角色已接入工作区和任务路径；成员移除后任务列表、直接访问及产物权限同步收紧。
- `_api_scope` 对知识库、记忆、专家团、摘要等请求检查工作区权限；自动化和通知使用服务端身份并保护直接 ID 访问。私有知识库、个人记忆和附件导入的伪造身份测试通过。
- 认证测试使用临时 SQLite；8 项测试覆盖认证撤销、10 个独立账号和任务/产物越权。
- 前端认证与诊断路由 6 项测试通过；未完成真实浏览器视觉验收。

## 仍需实现或修正

- 工作区成员界面已接入按用户名添加、切换权限和移除，后端继续限制所有者/管理员。真实浏览器验收及全接口权限审计仍待完成。组织级记忆/知识库的发布、修改和删除已限制管理员，记忆修订 actor 使用会话身份；专家共享配置及对话安装工具路径仍需审计。
- 管理员用户管理页面已接入创建、角色调整、停用和密码重置，前端行为测试通过；真实浏览器视觉验收待完成。已增加持久化账号/来源登录限流、HTTPS Secure Cookie、Origin/跨站写请求检查及审计事件：记录身份、方法、路由模板、状态，不存正文和凭据；异常退出时未完成审计行保留为未知结果。代理部署下的 Origin 和来源配置仍需部署验收。
- Redis 队列已改为 Streams 消费组，消息确认后删除，未确认消息可以重新领取；重试/死信与确认使用事务。独立 Redis 7 上通过 3 项集成测试。运行中的任务如何从检查点安全恢复仍未完成，不能将消息重领等同于业务恢复。
- Worker 锁增加所有者校验续租和释放；锁丢失时取消协程。外部进程或线程中的副作用无法仅靠取消协程隔离，需要进一步完善执行栅栏。
- 普通任务的 Task、Run、dispatch_outbox 已在同一事务提交；注入 Run 或 outbox 插入失败时全部回滚。重试和恢复也保留 outbox 原子性；后台重试未投递记录，Redis 按运行/审批 ID 去重。其他编排路径仍需逐项验收。
- 审批通过 approval_dispatch 与审批命令原子提交，由 Worker 续跑；真实进程测试覆盖拒绝 Skill 安装后继续完成原任务。Policy 审批也纳入投递，API 重启不消费 Worker 审批，Worker 持久化决定后创建带证明的恢复 Run；16 项恢复/outbox 测试通过。Policy 的真实进程崩溃演练及自动化仍需完善。
- 专家团创建事务写入团队投递记录，由 Worker 运行成员与主管汇总；API 重启排除 Worker 团队及其子任务。单成员重试已接入独立持久投递和 Worker，限制同团队活动重试唯一。32 项专家/outbox 回归及单独投递消费测试通过；重试和团队中断后的真实进程演练仍待完成。
- Worker 已在普通任务进入配额/模型执行前消费排队取消标记，必须有数据库取消命令才能取消；运行中仍由原有安全节点处理持久命令。Redis 通知失败不撤回已接受取消。13 项队列/取消测试通过。
- SSE 已接入 Redis 游标通知，正文仍从 SQLite 读取；超时补读保证未 publish 的历史事件仍可按游标恢复，持续连接复查会话/任务权限。29 项事件/进程/API 回归及单独的 Redis SSE 无通知补读测试通过。
- API 重启按持久化 dispatch_backend 排除 Worker 运行和相应工具执行。Worker 重领过期运行后可原子创建恢复运行及 outbox；重复恢复复用同一子运行。21 项恢复/工具日志/outbox 测试通过，尚未完成真实 Worker 进程终止后的端到端演练。
- API/Worker 共用 Compose 环境和数据挂载，Worker 支持扩容并等待 API 健康。配置已增加非 root、只读根文件系统、去除 capabilities、进程/CPU/内存限制和临时目录。已成功构建 Python 3.12 镜像，并通过独立 Compose 实例的非 root/只读验证、登录、上传、普通任务、Markdown 生成和下载。构建时修复了新增源码文件权限导致非 root 无法读取的问题。临时容器、网络和数据卷已清理。
- 备份改用 SQLite backup API，包含默认布局本机密钥和文件校验清单；恢复只允许新目录。4 项临时数据测试通过。自定义路径、外部密钥、Redis 消息与数据库恢复的一致性及真实部署演练仍待完成。
- 当前全量回归：`.venv/bin/python -m unittest discover -s tests -p 'test_*.py' -q`，571 项，567 通过、4 跳过，耗时 421.541 秒；前端脚本检查和 Python 编译也通过。本轮同时设置独立 Redis、Chromium 和测试镜像环境变量，实际执行了队列、Redis 重启、Worker 崩溃、浏览器和 Compose 恢复测试；4 项跳过为未配置的可选外部组件。新增后续改动后仍须相应复验。
- 完整 Compose、并发、断网、重启、备份恢复和试运行验收尚未完成。

## 2026-09-09 队列状态丢失补投验收

- 补投范围覆盖未开始的普通/团队 Run、待消费审批、自动化和成员重试；按 dispatch_id 或 run_id 去重，不接管执行中记录。
- 在独立 Redis 7 临时容器和临时 SQLite 上执行 `APP_TEST_REDIS_URL=redis://127.0.0.1:32796/0 .venv/bin/python -m unittest tests.test_task_queue tests.test_dispatch_outbox -q`：20 项全部通过，耗时 3.713 秒。测试端口仅供本次临时实例使用。
- 新增测试验证审批补投及 Run 开始后停止补投；自动化/成员重试只恢复 queued 记录，排除 running/completed/failed；重复扫描保持单次投递。首次夹具违反自动化活动记录唯一约束，改为独立资源后重跑通过。
- 此处验证的是持久消息重建，Policy/自动化审批中断后的业务收敛及最终统一验收仍未完成。上文较早的测试结果保留为历史记录，不代表当前代码全量验收。

## 2026-09-09 自动化审批终态同步

- 修复子任务审批后结束、父自动化及触发事件仍显示等待审批的问题。API 调度轮询从 SQLite 读取终态，原子更新轮次结果、输出状态、差异、触发事件及通知；无需依赖审批进程存活或 Redis 通知到达。
- 保留原调度模式，按目标完成、次数上限和失败熔断决定后续状态；手动暂停保持暂停，旧轮次不覆盖新轮次，不重复增加计数或发通知。旧数据缺少审批前调度模式时默认保持暂停，需人工重新启用。
- 对原调度进程尚未写完轮次计数的窗口暂缓同步，避免它随后覆盖结果。已被中断恢复暂停的自动化可补齐本轮计数和结果。
- `tests.test_loops tests.test_worker_permissions tests.test_restart_recovery`：34 项通过；增加交接窗口保护后复验 `tests.test_loops`：21 项通过，9.229 秒。Python 编译和 `git diff --check` 通过。覆盖幂等、重建调度器后同步、手动暂停、旧轮次晚到、事务回滚及交接等待；不是实际进程崩溃的端到端审批证明。
- 仍需完成 Policy/自动化审批真实进程中断演练，以及最终统一验收。

## 后续顺序

### 2026-09-12 智能体页面优化

- 智能体列表参考项目页面增加名称、ID、描述查询和“显示已停用”开关。
- 左侧智能体列表限制在视口内滚动，右侧编辑器保持同页固定并独立滚动。
- 右侧详情改为基础信息、系统提示词、能力配置、权限 JSON 四个紧凑分区，减少大块空白。
- 前端资源版本为 `20260912-agent-detail-4`，相关前端/Compose 测试通过；API、两个 Worker 已更新到镜像 `sha256:c67d43a1e41da76c7feffe6d025a9e92634dc3b2758b9e94248ef0e7df7f7929`，容器状态健康。

### 2026-09-12 项目页固定布局

- 根据实际截图，将项目页外层固定为视口高度，页面本身不滚动；左侧项目列表和右侧详情面板分别承担内部滚动，项目数量不会把详情推到页面下方。
- 右侧详情继续保留项目基础信息、默认智能体/模型、描述、JSON 设置、启停和成员管理，使用紧凑的同页编辑面板。
- 前端资源版本更新为 `20260912-workspace-layout-2`，解决浏览器缓存旧 CSS/JS 的问题。新镜像 `sha256:adeb44e3771abc1230364225e107afd42a0a6ffe01ac3ecc32abc156300526ee` 已部署，readiness 与两个 Worker 正常。
- `node --check web/app.js`、4 项相关测试、`git diff --check` 通过；生产页面已确认返回新资源版本和项目筛选元素。

### 2026-09-12 项目筛选界面缓存修复

- 截图显示搜索框已出现但列表仍由旧版脚本渲染，原因是浏览器缓存了固定查询参数的 `app.js`。现将 `app.js`、`styles.css` 和诊断脚本版本号更新为 `20260912-workspace-filter-1`，强制加载当前前端资源。
- 当前 `renderWorkspaces()` 会按名称、ID、描述过滤，并在未勾选“显示已停用”时排除 `enabled=false` 项目；左侧列表使用独立高度和滚动区域。
- 新镜像 `agentnexus-release-test` 已构建并更新到 API/两个 Worker，旧镜像保留为 `before-workspace-filter-20260912`。生产 API 返回新资源版本、项目筛选元素和 readiness 正常。
- `node --check web/app.js`、`tests.test_frontend_auth tests.test_release_compose`（4 项）及 `git diff --check` 通过。部署只重建 API/Worker，Redis、数据库和备份任务未改变。

### 2026-09-12 项目列表界面优化

- 项目列表增加按名称、ID、描述的前端即时查询；显示数量采用“匹配数/总数”形式。
- 增加“显示已停用”开关，默认隐藏停用项目；查询和开关只过滤当前已加载的数据，不改变服务器数据。
- 左侧列表设置独立最大高度和滚动区域，右侧项目详情保持同一页面的固定编辑面板，项目数量增加时不会把详情推到页面底部。
- `node --check web/app.js`、4 项前端/Compose 配置测试通过，当前镜像重建并部署后验证 API readiness（2 个 Worker）和页面包含 `workspaceSearch`、`workspaceShowDisabled`。

### 2026-09-09 当前业务代码完整验收与交接

- 镜像 `sha256:3f8399f6a13af467a8e48eb3cc9e6a828a9a49456cc73b2fa5867ad8fb48dfc3` 对应本轮业务代码。使用独立 API、Redis AOF、已有 Chromium 和 Python PPTX 生成器执行完整发现回归：622 项，618 通过、3 跳过、1 失败，582.219 秒。
- 唯一失败是旧接口测试在本地路径安装关闭时假定另一个测试已安装 meeting_notes_sample。修改测试使其自行上传既有 ZIP 夹具，未修改业务代码或不可变样例；全新实例单独测试通过，再按完整回归的顺序复跑全部 24 项旧接口测试，21 通过、3 跳过，88.123 秒。不能将原全量退出码 1 写成一次全绿。
- 新增双用户普通任务执行时间交叠断言，配额设为每用户 1，验证两个 Worker 同时执行不同用户任务。重新执行整个 `tests.test_worker_process` 通过，93.599 秒；其中普通崩溃恢复、Policy 审批故障转移和自动化审批全进程重建仍通过。
- 当前所有可执行测试中的已知失败已解决；三项真实在线模型/专家团旧接口场景仍缺配置，不计通过。证据位于 `/tmp/agentnexus-final-verification-86kfsx5d`，包括原始全量结果、单项及接口组复验记录；并发补验使用独立临时 Redis，结束后已清理。
- 前端语法、启动脚本、Python 编译及 `git diff --check` 通过。临时 API、Redis 和 Compose 资源已清理，无实际部署切换、定时备份安装或 Git 提交。
- `docs/SECOND_RELEASE_HANDOFF.md` 提供可审查的实际部署、停机快照、每日备份和试运行方案。剩余工作需要实际部署授权、外部模型配置及真实用户参与，不能以代码回归代替。

### 2026-09-09 后台上下文和服务端身份复核

- 新增真实 AgentRuntime + Worker deadline 入口的组合测试：准备 Alice/A、Bob/A、Alice/B 三组私有记忆和知识内容，在后台上下文放入 Bob 身份后执行持久记录属于 Alice/A 的任务；模型实际收到的上下文仅包含 Alice/A 数据。任务正常完成，未访问外部模型。
- 修复成员重试响应 scope 回显客户端原始 user_id/organization_id 的问题。执行路径原已使用服务端身份，现在响应也返回同一执行范围；请求伪造身份的测试验证鉴权校验、派发参数及响应一致。
- `.venv/bin/python -m unittest tests.test_auth_service tests.test_memory_api_runtime tests.test_knowledge_base -q`：28 项通过，46.338 秒。Python 编译和 `git diff --check` 通过。
- 手册补充计划名称与实际服务名、代码默认与多人示例值、SQLite/Redis 路径、成功/重试状态兼容映射、健康检查分工及协议版本字段的对应表。上述文档说明没有缩减原计划范围。
- 成员重试响应修复晚于上一镜像构建；最终交付前需刷新对应镜像证据。

### 2026-09-09 新镜像复验与管理路由矩阵

- 使用镜像 `sha256:7b5a9a67e33794f8695858f15ccd121298bfdf2a9aba01942f16f75836fd6805` 执行 `APP_TEST_IMAGE=agentnexus-release-test .venv/bin/python -m unittest tests.test_compose_process tests.test_release_compose -q`：2 项通过，50.562 秒。覆盖两 Worker、登录、上传、普通任务、Markdown 下载、停机快照、新目录恢复和容器重建；复原后的会话、任务与下载内容一致。
- 新增管理写接口矩阵测试，从当前 FastAPI 注册路由枚举 users/models/agents/skills/mcp/policies/marketplace/presentation 的写操作，确认普通成员在正文验证和副作用执行前统一收到 403；用户和审计列表同样拒绝访问。该测试 3.394 秒通过，临时 SQLite，无 lifespan、未触及用户运行库。
- 运行手册补齐真实服务名 agentnexus/worker/redis，以及状态、日志、停止、升级、备份和回滚顺序。上述部署命令本轮仅写入文档，未对实际服务执行。`git diff --check` 通过。

### 2026-09-09 自动化与成员重试超时原因一致性

- 新增自动化 deadline 回归，验证协程取消、任务/轮次/触发事件/投递结束、自动化暂停和消息确认。复现原先将超时标成 AutomationInterrupted、轮次通知标成服务重启的问题。
- Worker 使用自身 deadline 是否到期区分 TaskTimeout，续租权限撤销区分 PermissionRevoked；自动化恢复服务接受实际错误原因，任务、轮次、触发事件和通知保持一致。普通 API 启动恢复仍默认 ServiceRestart。
- 单成员重试也区分超时和权限撤销，并使用真实专家服务关闭父团队。测试验证超时后投递、团队及父 Task/Run 全部结束，终态检查通过。
- `tests.test_worker_deadline tests.test_worker_permissions tests.test_loops`：27 项通过，16.503 秒；成员重试补齐后 `tests.test_worker_deadline tests.test_worker_permissions tests.test_member_retry_dispatch`：8 项通过，5.894 秒。Python 编译、`git diff --check` 通过。
- 当前代码镜像已重建成功：`agentnexus-release-test`，ID `sha256:7b5a9a67e33794f8695858f15ccd121298bfdf2a9aba01942f16f75836fd6805`。此镜像尚需对应的 Compose 集成复验，构建成功不等于部署完成。

### 2026-09-09 专家团超时与中断收敛

- 审计发现通用 Worker deadline 只关闭父 Task/Run，会遗留团队及活动成员投影。增加专家服务的 `fail_interrupted_run`：逐个提交子任务失败与对应成员状态，再关闭无子任务的排队成员，最后使用既有团队终态事务发布父结果。
- Worker 专家团超时、进程中断后重领、执行前权限撤销，以及单成员重试中断均调用此清理路径。各子任务及父团队分别持久提交，未宣称整个任务树属于同一个数据库事务；重复完成清理不重复发布父任务事件。
- 新测试通过真实 ExpertTeamService 创建专家团，等待子任务开始后触发 Worker 超时，检查父 Task/Run、team_runs、team_member_runs 及子 Task/Run 全部收敛，并逐项运行终态残留检查。
- `tests.test_expert_teams tests.test_worker_deadline tests.test_worker_permissions tests.test_member_retry_dispatch`：34 项通过，32.315 秒；补充重复清理断言与权限撤销分支后针对性 6 项复验通过，4.834 秒。Python 编译及 `git diff --check` 通过。
- 后续仍需核对其他编排的超时边界及最终镜像/集成证据；不承诺撤销已经开始的远端、线程或进程副作用。

### 2026-09-09 外部调用超时和重试配置统一

- 增加共享配置读取 `call_limits.py`，统一资源配置优先于环境默认值、有限数字校验及上下界。模型默认 90 秒、HTTP 工具 30 秒、MCP 60 秒；stdio 保留历史 5～300 秒限制及 startup_timeout 回退。
- 模型重试上限默认 3 次额外请求，覆盖摘要和工具循环，非流式备用请求也占用该上限和持久模型预算。`max_retries=0` 确保只发送一次请求；仅 HTTP 429/5xx 自动重试，超时/传输错误不重发。
- 模型、HTTP 工具、远程 MCP 增加调用总时限，避免持续接收数据或协议初始化使操作无限等待。HTTP/MCP 的自动重试配置仅接受 0，明确拒绝盲目重放副作用。
- 同步 `.env.example`、`.env.release.example`、API/Worker 共用的 Compose 环境配置及运行手册。未安装依赖，未修改实际运行环境。
- 执行 `tests.test_call_limits tests.test_model_budget tests.test_model_gateway_streaming tests.test_model_semantic_judge tests.test_mcp_timeout tests.test_mcp_api_security tests.test_release_compose`：25 项通过，3.875 秒；Python 编译和 `git diff --check` 通过。新增真实 MockTransport/协议会话测试验证零重试、预算累计、总超时取消及 HTTP 写入不重复发送。
- 此改动晚于 584 项统一回归；后续需刷新镜像及相应集成证据，不能引用旧镜像证明当前网关实现。

### 2026-09-09 模型备用请求预算修复

- 流式模型请求连续返回 503 后，既有兼容逻辑会发送一次非流式备用请求；此前该请求绕过模型预算。现已在发送前调用持久预算预留，余额耗尽时明确失败，不再发出第四次请求。
- 新增 MockTransport 测试保留真实网关、HTTP 状态处理和 SQLite 计数：预算为 3 时三次流式失败后拒绝备用请求；预算为 4 时备用成功，实际请求和持久计数均为 4。修改前两项均失败，修改后通过，没有访问真实模型。
- `.venv/bin/python -m unittest tests.test_model_budget tests.test_model_gateway_streaming tests.test_model_semantic_judge -q`：11 项通过；Python 编译及 `git diff --check` 通过。
- 此改动晚于上节 584 项统一回归及镜像构建；不能把旧镜像当作包含本修复。下一步继续统一外部调用超时/重试配置，再按变更范围刷新验证。

### 2026-09-09 跳过项补验

- 为四组旧接口测试启动独立临时 API，所有数据写入 `/tmp/agentnexus-release-verification-fzs7me3_/live/`。对硬编码 8000 的测试类仅在本次 runner 内指定临时端口，没有连接或修改用户服务。
- 补跑 24 项：18 项通过，3 项因关闭外网而失败，3 项因未配置在线模型（2 项）或专家团（1 项）跳过。数据库错误明确显示天气查询被出站总开关拒绝；预检测试也明确断言该开关开启。
- 在另一独立临时实例开启外网，单独重跑三项失败测试：3 项全部通过，17.066 秒。天气查询实际访问公开天气服务；该配置仅用于临时实例，未修改运行环境或仓库配置。日志位于同验收目录的 `weather/`。
- 用已有 `APP_PPTX_GENERATOR=python` 补跑两项 PPTX 安全测试：2 项通过，1.435 秒，无新增依赖。验证了面向受众的内容、页数边界和超限内容明确拒绝。
- 因此前置配置缺失而未执行的三项在线模型/专家团场景仍不算通过；已有独立专家团 Worker 测试通过，但不能冒充这项旧测试已执行。临时 API 和 Redis 容器均已退出，证据目录保留。

### 2026-09-09 当前代码统一回归

- 构建镜像 `agentnexus-release-test`，ID 为 `sha256:77c3199255649a90d6da7c1355f26ded70a49782fc79b2d8f3de952acc7361f3`。Node 语法检查、`bash -n start-local.sh`、`compileall app scripts` 通过。
- 设置独立 Redis AOF 容器、已有 Chromium、当前测试镜像和临时默认数据路径，运行 `.venv/bin/python -m unittest discover -s tests -p 'test_*.py' -v`。报告 `Ran 584 tests in 482.790s / OK (skipped=6)`，退出码 0。浏览器、Compose 新目录恢复、Redis 重启、Worker 普通/Policy/自动化审批故障演练实际执行并通过。
- 6 条跳过记录包含 2 项可选 PPTX 组件测试及 4 组未连接本地服务的旧接口测试；整类 setUpClass 跳过不能与 testsRun 简单相减算通过数。四组旧接口测试正通过独立临时服务补跑。
- 原始证据保存在本机 `/tmp/agentnexus-release-verification-fzs7me3_/tests.log` 和 `result.json`，浏览器产物位于同目录 `browser/`。临时目录不属于仓库，可能被系统清理，正式交接前应按需要另行留存。
- 本轮审计发现的真实缺口：模型流式失败后的备用 POST 未计入模型预算；模型/HTTP/MCP 的超时与重试配置尚未统一。回归通过不代表这两项已完成。

### 2026-09-09 Policy 故障恢复修复与复验

- 修复普通运行锁租约与用户并发配额租约不一致：此前测试运行锁为 2 秒，而崩溃 Worker 配额固定占用 60 秒，连续故障使审批恢复在配额领取前等待。现在普通任务/审批的配额跟随运行锁，自动化/成员重试继续跟随固定 60 秒锁。
- 共享用户配额键的 TTL 只延长、不因短任务缩短，防止短租约任务导致其他长任务的有效占用提前丢失。真实 Redis 测试覆盖过期回收、旧令牌不能复活、混合租约不突破用户并发上限。
- 专家团偶发失败已捕获到运行租约丢失及“专家团执行中断”记录。进程验收使用 10 秒租约，避免 2 秒配置下同步初始化/SQLite 竞争干扰正常场景；仍显式终止 Worker 并验证回收，生产默认 60 秒不变。
- `APP_TEST_REDIS_URL=redis://127.0.0.1:32798/0 .venv/bin/python -m unittest tests.test_task_queue tests.test_worker_permissions -q`：19 项通过。随后 `tests.test_worker_process`：1 项复合进程测试通过，92.152 秒，覆盖普通/团队/自动化执行、API 重启、普通 Worker 检查点恢复、自动化审批时重建全部进程、Policy output.before 等待时终止所属 Worker 后故障转移。
- Policy 恢复断言：两个 Run、一个最终回答，恢复 Run 携带审批证明。自动化断言：一个完成轮次、一个完成触发事件、一个最终回答和一个同步通知。上节所述 Policy 审批决定未消费的失败已由本次实际测试解决，不代表所有外部工具和故障时刻均已验收。
- Python 编译及 `git diff --check` 通过；尚需最终统一验收及上线交接。

### 2026-09-09 审批真实进程验收与新发现

- 自动化 Skill 审批场景已通过真实进程测试：等待审批后终止 API 和全部 Worker，重启 API、提交拒绝安装但继续任务的审批，再启动两个 Worker；本轮最终完成，轮次、最终回答和同步通知均只有一份。首次完整进程测试 63.043 秒通过。
- 扩展 Policy 审批场景发现独立 Worker 未加载数据库规则，导致任务跳过审批。已修复为每次取得配额后调用既有规则加载器。修复后真实进程任务能进入 Policy waiting_approval；`tests.test_policy_api tests.test_worker_supervisor tests.test_worker_permissions` 共 8 项通过。
- **尚未通过**：Policy output.before 等待时终止所属 Worker、提交审批后，剩余 Worker 在 40 秒内未完成恢复。只读观察原 Run 仍为 waiting_approval、pending_policy_approval 仍存在、无 policy_approval_decisions、无恢复 Run。需继续检查审批投递/消费、锁及配额，而非仅增加超时时间。
- 扩展测试期间出现一次原有专家团失败，其后两次未复现；首次输出没有完整错误，已补充团队 error_json 和进程日志诊断。不能把未复现等同于已修复。
- `tests/test_worker_process.py` 保留失败场景；超时诊断最初误用 task_commands.type，已按实际 schema 改为 command_type。最终进程测试仍未通过，下一步先收集完整的命令和 Redis pending/锁证据。

### 2026-09-09 执行期间权限撤销修复

- 审计发现自动化和专家单成员重试只在开始执行前检查权限，续租期间未复查。现已与普通 Worker 路径一样在续租时检查当前账号、工作区成员及 Agent 可见性；自动化同时检查组织归属。
- 撤销权限后取消执行协程，成员重试走团队失败收敛，自动化关闭中断轮次、暂停后续调度并标记投递失败，持久化后确认消息。已有线程、子进程或外部请求的副作用不因协程取消而自动撤销。
- 新增测试使用临时 SQLite，在执行已开始后停用账号，模拟下一次续租，断言首次续租即取消（排除总超时触发的伪通过）、投递失败和消息确认；自动化同时检查任务、轮次、触发事件失败及父自动化暂停。
- `.venv/bin/python -m unittest tests.test_worker_permissions tests.test_member_retry_dispatch tests.test_loops -q`：20 项通过；加强续租次数断言后执行 `tests.test_worker_permissions tests.test_member_retry_dispatch tests.test_worker_deadline tests.test_worker_supervisor`：8 项通过。Python 编译及 `git diff --check` 通过。
- 仍待：自动化任务审批结束后的父轮次状态同步审计、Policy 审批中断演练及最终统一验收。

1. 补全资源权限及工作区成员关系，并用双用户越权测试覆盖各入口。
2. 解决队列消息确认、运行租约和 API/Worker 启动恢复所有权。
3. 统一专家/审批/自动化的 Worker 路径，再接入实时事件和取消通知。
4. 完善备份恢复、配置与隔离，在临时环境进行故障演练。
5. 回归及逐项上线审计；真实用户试运行结果单独记录，不以单元测试代替。
