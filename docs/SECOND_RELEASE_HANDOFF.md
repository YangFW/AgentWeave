# 第二次上线交接清单

本清单保留原计划全部范围。代码验证、实际部署和真实用户试运行分别记录；测试通过不能替代后两项。2026-09-09 用户明确授权后，已完成实际部署切换和定时备份安装，详见 `SECOND_RELEASE_DEPLOYMENT.md`。没有提交 Git，真实用户试运行尚未完成。

## 已执行的实际部署方案

| 项目 | 建议执行值 |
| --- | --- |
| 项目目录 | `/home/rickie/AgentNexus` |
| 运行数据目录 | `/home/rickie/AgentNexus/data`，切换前再次核对实际写入进程及自定义路径 |
| 配置文件 | 已从 `.env.release.example` 创建 `.env.release`，权限 0600 |
| 身份与并发 | 启用认证，两个 Worker，每用户一个并发执行 |
| 数据存储 | 保留 SQLite，Redis 使用 Compose 持久卷 |
| 当前访问入口 | 用户已指定 `192.168.0.230:8000`，已绑定该内网地址 |
| 外部调用 | 默认关闭出站网络及远程/stdio MCP，按实际模型和工具需要配置 |
| 管理员 | 使用已修复的既有 admin，不再次重置；不将密码写入本文件 |
| 当前部署镜像 | `sha256:f6e62c59b05ee20f542dcb9bdbddb6d43038a7e68bb5e999b0ac9853bf915588`，2026-09-10 附件与超时提示补丁 |

## 实际切换顺序

1. 确认维护窗口及部署目标，核对端口、实际数据路径、现有 AgentNexus 写入进程和磁盘空间。
2. 停止新任务提交，等待执行结束或确认允许中断；只停止属于本项目的 API/Worker。
3. 使用 `backup-data.sh --quiesced` 生成数据库、附件、产物和本机配置快照，记录输出路径并执行清单校验。管理员修复时的数据库备份不能代替此次完整文件快照。
4. 写入经确认的 `.env.release`，使用匹配代码与镜像启动 Compose 的 agentnexus、worker、redis 服务。既有用户密码不会被初始化环境变量覆盖。
5. 核对 readiness、管理员登录、普通用户权限、普通任务、专家任务、取消与重试、文件下载和日志。
6. 保留旧代码/镜像、旧数据和升级前快照；失败时停止新服务，先在新目录恢复并验证，再经确认切换目录。

具体命令见 `SECOND_RELEASE_RUNBOOK.md`。本清单中的部署切换及每日 03:00 停机备份已另获用户明确授权，不需要重复请求同一授权；其他超出已确认方案的高风险操作仍按 AGENTS.md 执行。

## 定时备份建议

建议每日北京时间 03:00 执行维护窗口备份。当前宿主机时区已核对为 CST +0800，存在 crontab 命令；实际安装前仍须复核。

本机暂存备份命令可审查为：

```cron
0 3 * * * AGENTNEXUS_BACKUP_DIR=/home/rickie/AgentNexus/backups /bin/sh /home/rickie/AgentNexus/scripts/backup-maintenance.sh --maintenance-window-approved
```

该任务会短暂停止并恢复原先运行的 API/Worker，每日停机窗口已获用户授权。已安装到 crontab，并追加写入 `backups/maintenance.log` 的日志重定向。本机 backups 只能作为初步保留位置，不能抵御整盘故障；需要确定独立磁盘或外部备份目的地。脚本不自动删除旧快照，需关注磁盘容量。

## 模型与试运行

- 真实在线模型测试需要在管理页面配置可用模型；不要把 API Key 发送到聊天或写入文档。
- 既有三项在线模型/专家团旧接口测试仍缺前置配置，不能用确定性模型冒充真实模型验收；普通及专家 Worker 的确定性执行已独立验证。
- 按原计划先邀请两名真实用户试用 3～5 天，记录任务成功率、排队时间、失败原因、权限和文件交付问题。
- 处理发现的问题后扩展到五人；连续稳定至少一周后再开放给全部十人。该时间与真实反馈不能用自动化测试缩短或替代。

## 当前验收记录

完整自动化回归在本机隔离环境执行 622 项：618 通过、3 跳过、1 项旧接口夹具顺序依赖失败。修正该测试自行准备 ZIP 夹具后，在全新实例按相同顺序复跑全部 24 项旧接口测试，21 通过、3 跳过；原失败已解决，业务代码未因该测试问题改动。浏览器、Redis 重启、Worker 故障恢复和 Compose 恢复实际执行并通过。

日志目录为 `/tmp/agentnexus-final-verification-86kfsx5d`，完整结果见 `result.json`、`tests.log`，顺序修复后的接口复验见 `live-rerun/result.json` 与 `live-rerun/tests.log`。不能将原全量执行的退出码 1 写成一次全绿。临时目录可能被系统清理，正式交接时应保留所需证据。
